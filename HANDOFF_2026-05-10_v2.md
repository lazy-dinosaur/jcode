# Lazydino Jcode — Session Handoff v2 (2026-05-10 13:34 UTC)

> 이 문서를 새 jcode 세션의 **첫 prompt 로 붙여넣으세요**.
> 이전 세션 (`session_mouse_1778416384546_b20646bdb7e456c9`) 의 컨텍스트를 모두 이어받습니다.

---

## 빠른 컨텍스트 흡수

작업 디렉토리: `/home/lazydino/dev/jcode`
브랜치: `patch/altb-wake-parent`
현재 binary: `~/.local/bin/jcode v0.12.126-dev (4e3d8189, dirty)` ← 모든 patch 포함

이전 세션이 한 일:
- M5/M1/M3 라이브 검증 PASS
- **M6** (Alt+B idle wake parent) DONE — `f2c8430f` + `074dcbef`
- **M7** (메시지 유실) DONE — `5f597b98` + `2951ebfb` + `patch/journal-stub-recovery`
- **M8** (Alt+B detach 후 turn 안 끝남) DONE — `4e3d8189` + `60332042` + `patch/altb-end-turn`
- M8 라이브 검증 PASS (큐 메시지 dispatch 확인)
- M8 검증 도중 새 이슈 5개 발견 → M12~M15 + M14a 등록

지금 시점 마일스톤:

| ID | 상태 | 한 줄 |
|---|---|---|
| M1 | ✅ | BG delivery routing |
| M2 | 🔴 | Swarm 버그 (증상 미확정) |
| M3 | ✅ | Hook 시스템 추가 (`session.stop`, `response.completed`) |
| M4 | 🟡 | TUI interleave (BY-DESIGN) |
| M5 | ✅ | Alt+B early race |
| M6 | ✅ | Alt+B idle wake parent |
| M7 | ✅ | 비정상 종료 시 메시지 유실 |
| M8 | ✅ | Alt+B detach 후 turn 안 끝남 |
| M9 | 🔴 | Hook 이중 발동 (`~/.jcode/config.toml` 자기참조) |
| M10 | 🔴 | non-blocking lifecycle hook race (`jcode run`) |
| **M11** | 🔴 | Lifecycle hook decision 무시 (5단계 patch 필요) |
| **M12** | 🔴 | Anthropic provider 가 ToolSearch 광고만 + 미구현 |
| **M13** | 🔴 | `schedule` (ScheduleWakeup) schema 미스매치 (`task` 필드) |
| **M14** | 🔴 | `/compact` 명령 미동작 |
| **M14a** | 🔴 | **Emergency compaction 무한 루프 (Critical)** |
| **M15** | 🔴 | 이미지 첨부 안 들어옴 (alt-text 만) |

상세는 `LAZYDINO_MILESTONES.md` 의 row 단위로 모두 기록되어 있음.

## 사용자 합의된 진행 순서

> **M13 → M14 → M12 → M11 → M15**
> (M14a 가 Critical 이지만 사용자가 "빠른 win 부터" 선호 — M14a 는 별도 진단 후 진행 권장)

이전 세션 마지막 메시지에서 "그리고 무한대로 compact 하는 버그도 있어" 로 M14a 등록 직후 handoff 요청. M14a 우선순위는 새 세션에서 결정.

## 다음 행동 (새 세션에서)

### 1. 환경 확인 (3 분)

```bash
# binary 확인
~/.local/bin/jcode --version
# 기대: v0.12.126-dev (4e3d8189, dirty) 이상

# 활성 server 확인 (절대 kill 금지)
for pid in $(pgrep -f "jcode .*serve"); do
    echo "PID $pid: $(readlink /proc/$pid/exe) ($(ps -p $pid -o etime= | tr -d ' ') elapsed)"
done

# 마일스톤 문서 빠르게 정독
head -40 /home/lazydino/dev/jcode/LAZYDINO_MILESTONES.md
```

### 2. M13 부터 시작 (가장 빠른 win, ~30분)

**M13 — schedule (ScheduleWakeup) schema 미스매치**

증상: AI 가 `schedule` tool 호출하면 `Error: missing field 'task'`

원인:
- `src/provider/anthropic.rs:893-896` 에서 schema 정의
  ```json
  {"properties":{"delaySeconds":{"type":"number"},"reason":{"type":"string"},"prompt":{"type":"string"}},"required":["delaySeconds","reason","prompt"]}
  ```
- 그런데 AI 가 실제로 보내는 payload 는 `task` 필드를 포함

조사할 것:
1. `src/tool/ambient.rs:830` `impl Tool for ScheduleTool` — 실제 dispatch 핸들러
2. `ScheduleTool::parameters_schema()` 가 뭘 기대하는지 vs `anthropic.rs:895` 의 광고 schema
3. AI 가 실제 호출 시 보내는 input JSON (디스크 journal 에서 확인 가능)

Fix 방향 (예상):
- 광고 schema 와 실제 dispatch schema 일치시키기 (한쪽이 stale)
- 회귀 테스트: schema 가 일치하는 unit test 1개 추가

### 3. M14 (compact 미동작) 진단

증상 미확정. 사용자한테 reproduction 요청 + 코드 추적.

조사 시작점:
- `src/compaction.rs`
- `/compact` 명령 핸들러 (TUI 측)
- compaction trigger 경로

### 4. M14a (compaction 무한루프) — Critical

라이브 화면 증거:
- 3회 연속 emergency compaction
- 매번 504k → 20k → 또 504k 패턴
- dropped messages 카운트 증가 (562 → 564 → 566)

원인 가설:
- system prompt + tool defs 가 너무 커서 압축 불가
- summary 가 다시 prompt 로 expand 됨
- compact loop 종료 조건 부재

조사:
- `src/compaction.rs` 의 trigger threshold + loop 조건
- Anthropic provider 의 prompt build 코드 — 실제 token count 확인
- Loop 안전망 (max_consecutive_compactions 같은 거 있는지)

### 5. M12 → M11 → M15 순서로 진행

각 마일스톤 본문 참고.

## 작업 방식 / 컨벤션

- 모든 fix 는 `patch/<short-name>` 브랜치에 만들고 cherry-pick 으로 stack 유지
- commit 둘로 분리: (a) `fix:`/`feat:` 코드+테스트, (b) `docs:` 마일스톤/플랜 갱신
- 패치 등록 절차:
  1. `LAZYDINO_MAINTENANCE.md` 에 patch 항목 추가
  2. `scripts/lazydino/reapply-custom-stack.sh` PATCH_STACK 끝에 추가
  3. `LAZYDINO_MILESTONES.md` row 갱신
  4. 빌드 + 배포 (`cp target/release/jcode /tmp/x && mv /tmp/x ~/.local/bin/jcode` — atomic)
- 회귀 검증: `cargo test --lib <area>` (전체 `cargo test` 는 5분 timeout 위험)
- 라이브 검증: 새 jcode session 으로 직접 시연 + 결과 `LAZYDINO_TEST_PLAN.md` 에 기록
- `git -c user.name=lazydino -c user.email=lazydino@local commit` 로 커밋

## 주의사항

- ⚠️ **활성 server 절대 kill 금지** (PID 153400, 1466297, 1770980 등 — 4시간 ~ 51분 elapsed 인 process). binary 교체는 반드시 `cp + mv` (atomic) 로.
- 새 patch 효과는 **새 jcode session** 에서만 보임. 기존 server 는 옛 binary 를 메모리에 들고 있음.
- 사용자 동시 편집 충돌: `LAZYDINO_MILESTONES.md` 는 사용자가 자주 직접 편집 중. row 단위 작은 edit 로 처리.
- subagent 호출 시 사용자가 Alt+B 로 background 보내는 경우 자주 있음 (지금 M8 fix 동작 확인됨). bg 결과는 자동 도착.
- 이미지 첨부는 가끔 됨/가끔 안 됨 (M15). 안 들어오면 사용자한테 텍스트 보고 부탁.

## 라이브 검증 데이터 위치

- 세션 journal: `~/.jcode/sessions/session_<short>_<ts>_<id>.journal.jsonl`
- 세션 snapshot: `~/.jcode/sessions/session_<short>_<ts>_<id>.json`
- 로그: `~/.jcode/logs/jcode-<date>.log`
- bg task 결과: `/tmp/jcode-bg-tasks/<task_id>.output` + `.status.json`

## 첫 응답에서 해야 할 것

1. 위 환경 확인 명령 3개 실행, binary 4e3d8189 (또는 더 최신) 인지 확인
2. `LAZYDINO_MILESTONES.md` row 표 (1~35 줄) + M14a 본문 (라이브 화면 증거) 정독
3. 사용자에게 인사 + **"M13 부터 시작할까요? 아니면 M14a (Critical) 먼저?"** 물어보기
4. 사용자 결정 후 진행
