# Session 2026-05-11 — M19 + M20 deployed (FINAL)

> 한 세션 분량의 진행 기록. **이 문서 + `HANDOFF-2026-05-11.md` 만 읽으면**
> 다음 세션이 즉시 이어서 작업할 수 있도록 모든 산출물·branch·검증 결과·
> blocker·다음 단계를 한 곳에 모았다.

## TL;DR

- ✅ **M19 (config hot-reload) + M20 (bash tool timeout) 모두 완전 배포**
- ✅ **`deploy/m9-m10` tip = `da4fe48f`** — M9, M10, M19, M20 통합본
- ✅ **binary `lazydino-da4fe48f`** 가 `~/.local/bin/jcode` 와
  `~/.jcode/builds/{stable,current}/jcode` 에 atomic deploy
- 🟡 사용자가 **TUI close+reopen 해야** 새 binary 가 server process 에 잡힘
  (현재 떠 있는 9개 server 는 모두 옛 `lazydino-82b7c81f` 메모리에 들고 있음)
- 🔴 다음 작업: **M11 (lifecycle hook decision parsing)** 또는
  **M15/M17/M18** 중 우선순위 협의

## 1. 최종 branch 상태

| Branch | tip commit | 의미 |
|---|---|---|
| `master` | `4b97d322 Improve Bedrock credential error messages` | upstream 동기화점 |
| `deploy/m9-m10` | `da4fe48f test(m19): cover hot-reload, last-good fallback and recovery` | **M9 + M10 + M19 + M20 통합 deploy** |
| `patch/config-hot-reload` | `bdf63f5c test(m19): ...` | M19 source (master 베이스) |
| `patch/bash-tool-timeout` | `10db6a96 test(m20): ...` | M20 source (master 베이스) |
| `patch/lifecycle-hooks` | `5d81c901 docs: ...` | M11 작업 예정 (`~/dev/jcode-m11` worktree) |

```
master ─── 4b97d322
   ├── patch/config-hot-reload ── 93282b44 ── bdf63f5c
   ├── patch/bash-tool-timeout  ── 11edbb91 ── 10db6a96
   └── deploy/m9-m10
        ├── ... (M9, M10, docs)
        ├── 28b524ba fix(m20):    ← cherry-pick 후 conflict 풀고 통합
        ├── e6f98968 test(m20):
        ├── 96445a80 fix(m19):    ← cherry-pick 후 conflict 풀고 통합
        └── da4fe48f test(m19):   ← 수동 append (cherry-pick 충돌 회피)
```

## 2. 이번 세션 commit 목록 (deploy/m9-m10 기준)

```
da4fe48f test(m19): cover hot-reload, last-good fallback and recovery
96445a80 fix(m19): hot-reload config on mtime change
e6f98968 test(m20): cover resolve_timeout_ms precedence and BashToolConfig clamping
28b524ba fix(m20): make bash tool timeout configurable, raise default to 5min cap to 20min
e51fa9f6 docs: register M19 (DONE, integration pending) + M20 (bash 2min timeout)
```

(`f9883ee2` 이후 모두 이번 세션 산출물)

## 3. 검증 결과

### M19 (5/5 passed)

```
$ cargo test --lib config::tests::test_m19 -- --test-threads=1 --nocapture
running 5 tests
test config::tests::test_m19_config_initial_load_after_reset ... ok
test config::tests::test_m19_config_keeps_last_good_on_invalid_toml ... ok
test config::tests::test_m19_config_recovers_after_invalid_then_valid_write ... ok
test config::tests::test_m19_config_reloads_when_mtime_changes ... ok
test config::tests::test_m19_force_reload_is_noop_when_unchanged ... ok
test result: ok. 5 passed; 0 failed
```

### M20 (5/5 passed)

```
$ cargo test --lib tool::bash::tests::test_m20 -- --test-threads=1 --nocapture
running 5 tests
test tool::bash::tests::test_m20_bash_tool_config_clamps_pathological_values ... ok
test tool::bash::tests::test_m20_resolve_timeout_caps_explicit_request ... ok
test tool::bash::tests::test_m20_resolve_timeout_passes_small_explicit_through ... ok
test tool::bash::tests::test_m20_resolve_timeout_uses_config_default_when_unspecified ... ok
test tool::bash::tests::test_m20_schema_description_mentions_5min_default_and_20min_cap ... ok
test result: ok. 5 passed; 0 failed
```

### M9 회귀 (2/2 passed)

```
$ cargo test --lib config::tests::test_hooks_for_working_dir -- --test-threads=1
test config::tests::test_hooks_for_working_dir_dedupes_when_global_path_equals_project_path ... ok
test config::tests::test_hooks_for_working_dir_still_merges_distinct_project_path ... ok
test result: ok. 2 passed; 0 failed
```

### Release build

```
$ cargo build --release
Finished `release` profile [optimized] target(s) in 2m 49s
target/release/jcode = 94 088 152 bytes (94 MiB)
```

## 4. 배포 상태

```
$ ls -la ~/.local/bin/jcode ~/.jcode/builds/{stable,current}/jcode
-rwxr-xr-x  ~/.local/bin/jcode -> (94 MiB binary, lazydino-da4fe48f)
lrwxrwxrwx  ~/.jcode/builds/current/jcode -> ~/.jcode/builds/versions/lazydino-da4fe48f/jcode
lrwxrwxrwx  ~/.jcode/builds/stable/jcode  -> ~/.jcode/builds/versions/lazydino-da4fe48f/jcode
```

| 떠 있는 server PID | binary in memory | 비고 |
|---|---|---|
| 1466297, 1770980, 1774920, 2016680, 2185491, 2398583, 2410241 | `lazydino-82b7c81f` | TUI close+reopen 시 새 binary |
| 2751382, 2804808 | `~/.local/bin/jcode (deleted)` | 이미 옛 binary 메모리. close+reopen 필요 |

## 5. 구현 디자인 (요약)

### M19 (config hot-reload)

- `OnceLock<Config>` → `OnceLock<Mutex<ConfigCache>>`
- 매 `config()` 호출에서 `stat()` → mtime 비교 (500ms debounce)
- 변경 시 `Config::try_load()` → 성공 시 `Box::leak` 으로 새 `&'static`
- 실패 시 last-good 유지 (warn 만 로깅, 캐시 poison 안 됨)
- public `&'static Config` signature 100% 보존 (callers 무수정)
- 신규 API: `force_reload_config()`, `reset_config_cache_for_tests()` (cfg(test))
- 신규: `Config::try_load() -> anyhow::Result<Self>`
- `set_default_model` 의 옛 OnceLock 우회 워크어라운드 제거
  (`force_reload_config()` 한 줄로 대체)

### M20 (bash tool timeout)

- 새 `crates/jcode-config-types/src/lib.rs::ToolConfig` / `BashToolConfig`:
  - `default_timeout_ms` (default 300_000ms = 5min)
  - `max_timeout_ms` (default 1_200_000ms = 20min)
  - `HARD_CAP_MS = 20min` (절대 상한, config 무시)
  - `effective_default_ms()` / `effective_max_ms()` clamp helper
- `Config::tool: ToolConfig` field 추가
- `src/tool/bash.rs::resolve_timeout_ms(Option<u64>) -> u64`:
  - `requested` 있으면 `clamp(1, max)`
  - 없으면 `default.min(max)`
  - config 읽기는 `crate::config::config()` → M19 hot-reload 자동 적용
- 세 사용처 (foreground/agent-turn/background) 모두 helper 통합
- schema `timeout` description 에 5min default / 20min cap / `[tool.bash]`
  knob 안내 → 모델 (즉, 우리 미래 세션) 이 long-running 시 명시적으로
  `timeout: 600000` 같이 주는 게 권장됨

### 사용자 옵션 (필요시 ~/.jcode/config.toml 에 추가)

```toml
[tool.bash]
default_timeout_ms = 600000   # 10분으로 default 더 늘리기
max_timeout_ms     = 1200000  # 20분 (이미 default)
```

(M19 hot-reload 덕분에 저장 후 다음 tool call 부터 적용. 단,
**현재 떠 있는 server 는 옛 binary 라 안 됨** — close+reopen 필요.)

## 6. 디버깅 흔적 (다음 세션 팁)

- M19 fix cherry-pick → deploy 깨끗.
- M19 test cherry-pick → `src/config_tests.rs` 충돌. 단순 sed 로 마커
  지우면 brace 손상 (`} - unclosed delimiter`). **해결법: cherry-pick
  abort 후, M19 test 의 + 줄들 (`git show bdf63f5c | awk` 로 추출 가능)
  을 deploy 끝에 단순 append.** 두 영역 (M9 / M19) 가 식별자 충돌 없는
  순수 추가라 이게 가능했음.
- M20 cherry-pick → `src/config.rs` 충돌. 두 군데 모두 양쪽 다 keep
  union (import 합치기, struct field 둘 다 유지) 으로 깔끔히 해결.
- bg tool 2분 timeout (M20 의 원인) 때문에 cargo test/build 마다
  `nohup ... > /tmp/log 2>&1 & disown` 패턴 + 빠른 폴링 필요했음.
  이제 M20 fix 가 release binary 에 들어가 있어서 **다음 세션의
  TUI 가 close+reopen 후엔 이 패턴 거의 불필요**.

## 7. 다음 세션 작업 후보 (우선순위 제안)

1. **M11 (lifecycle hook decision parsing)** — `~/dev/jcode-m11` worktree,
   `patch/lifecycle-hooks` 브랜치. stages 1+2 부터.
2. **M15/M18 (이미지 데이터 전송)** — M18 이 작은 SDK 추가.
3. **M17 (live turn handoff to swarm)** — 사용자 워크플로우 이슈, 설계
   결정 필요 (A vs B vs B1).
4. **M16 (Anthropic OAuth tool advertising 통일)** — 구조 개선, 회귀
   방지.
5. **M2 (swarm 버그)** — 증상 미확정, 재현부터.

## 8. 추가 참고 파일

- `LAZYDINO_MILESTONES.md` (M19/M20 표 갱신 완료)
- `LAZYDINO_LIVE_VERIFICATION_M9_M14a.md` (이미 M9~M14a 검증 plan
  있음, M19/M20 도 이 형식으로 추가 가능)
- `docs/lazydino/analysis/M11-stages-1-2.md`,
  `docs/lazydino/analysis/M11-stages-3-5.md`,
  `docs/lazydino/analysis/M19-config-hot-reload.md` (M19 결정 배경)
- `docs/lazydino/sessions/2026-05-11-m19-impl.md` (이 세션의 중간
  진행 노트, 통합 미완 시점에 작성 — 본 final 노트로 superseded)
- `docs/lazydino/sessions/HANDOFF-2026-05-11.md` (다음 세션
  복붙용 prompt)
