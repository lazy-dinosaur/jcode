# HANDOFF — 2026-05-11 → 다음 세션

> 다음 세션 시작 시 **이 파일 전체를 복사해서 첫 메시지로 붙여넣으세요.**
> 한국어로 진행. fix/docs/test commit 분리. lazydino author identity.

---

## 📌 즉시 실행해서 상태 검증 (5초)

```bash
cd /home/lazydino/dev/jcode && \
git -C . log --oneline deploy/m9-m10 -5 && \
echo "---" && \
ls -la ~/.local/bin/jcode ~/.jcode/builds/{stable,current}/jcode 2>&1 | head -3 && \
echo "---" && \
readlink ~/.jcode/builds/current/jcode
```

**기대값:**
- 최상단이 `docs(handoff):` 또는 `docs(m21):` commit (HANDOFF 갱신
  사이클에 따라 hash 바뀌지만 메시지는 안정적)
- 그 아래 안정 commit 순서: `f50584a3 docs(m21): ...` →
  `4dc0a114 docs(m19,m20): ...` → `da4fe48f test(m19): ...` →
  `96445a80 fix(m19): ...` → `e6f98968 test(m20): ...` →
  `28b524ba fix(m20): ...`
- `~/.local/bin/jcode` 가 94 MiB binary (lazydino-da4fe48f)
- symlink 들이 `versions/lazydino-da4fe48f/jcode` 가리킴
- `git tag -l 'backup/pre-upstream-rebase-20260511-0136/*' | wc -l` → **48**

## ✅ 직전 세션 (2026-05-11) 완료 사항

1. **M19 (config hot-reload)** — `~/.jcode/config.toml` 편집이 process
   restart 없이 500ms 내 반영. last-good fallback. test 5/5.
2. **M20 (bash tool timeout)** — 2min 하드 timeout → 5min default /
   20min cap. `[tool.bash]` config 노출. helper 통합. test 5/5.
3. **deploy/m9-m10 tip = `f50584a3`** (M9 + M10 + M19 + M20 + M21 docs).
4. **release binary 배포 완료** (`~/.local/bin/jcode` +
   `~/.jcode/builds/{stable,current}/jcode` → `lazydino-da4fe48f`).
5. **M21 (upstream rebase + dedupe) milestone 등록** — fork/master 가
   origin/master 보다 226 commit 뒤져서 push 막힘. 자세한 절차는
   `docs/lazydino/sessions/2026-05-11-m19-m20-deployed.md` §9.
6. **백업 tag 48개** (`backup/pre-upstream-rebase-20260511-0136/*`) —
   deploy/m9-m10 + 47 patch 모두 보호.

## 🟡 사용자 액션 대기

**현재 떠 있는 jcode server 9개 모두 옛 binary (`82b7c81f`) 메모리에
들고 있음**. 새 fix 효과 보려면 **TUI close+reopen 필요**. 그 전엔:

- M19 효과 검증 불가 (config 편집 반영 안 됨)
- M20 효과 검증 불가 (bash tool 여전히 2min 에서 끊김)

## 📋 다음 작업 후보 (우선순위 제안)

| 순위 | 마일스톤 | 위치 | 비고 |
|---|---|---|---|
| 1 | **M21 (upstream rebase + dedupe)** ★ NEW | tag `backup/pre-upstream-rebase-20260511-0136/*` 보호 아래 | fork/master 가 origin/master 보다 226 commit 뒤짐. 그 안에 M14/M14a/M7 와 같은 영역의 upstream fix 다수 + 대규모 'Extract/Move into core' refactor 30+ 건. 먼저 처리 안 하면 path mismatch 로 다음 fix 들도 충돌 폭탄. PAT `workflow` scope 갱신 → fork/master ff push → deploy/m9-m10 rebase 후 dedupe → patch 별 push. 2-4h |
| 2 | **M11 (lifecycle hook decision parsing)** | `~/dev/jcode-m11`, `patch/lifecycle-hooks` | 분석 완료 (`docs/lazydino/analysis/M11-stages-{1-2,3-5}.md`). stages 1+2 부터 |
| 3 | **M18 (SDK get_history image bytes)** | master 베이스 새 patch | M15 의 가장 좁은 fix. ~10 LOC. 작은 win |
| 4 | M15 (image data 전송 본체) | master 베이스 | M18 이 끝나야 의미 있음 |
| 5 | M17 (live turn handoff to swarm) | 미설계 | A/B/B1 결정 필요. 사용자 워크플로우 직접 영향 |
| 6 | M16 (Anthropic OAuth tool advertising 통일) | 구조 개선 | 회귀 방지 가치, 급하진 않음 |
| 7 | M2 (swarm 버그) | 증상 미확정 | 재현부터 |

**제안 시작점**: M21 부터. upstream 동기화 안 한 상태로 새 patch 더
쌓으면 path mismatch 누적되어 결국 같은 충돌을 더 큰 규모로 해결하게 됨.
사용자 PAT 갱신 끝나면 진행.

## 🔒 백업 (이번 세션 종료 전 찍음)

**tag prefix**: `backup/pre-upstream-rebase-20260511-0136/`

48개 tag (deploy/m9-m10 + 47 patch). rebase 가 어그러지면:
```bash
git checkout -B deploy/m9-m10 backup/pre-upstream-rebase-20260511-0136/deploy-m9-m10
# 또는 patch 별:
git checkout -B patch/<name> backup/pre-upstream-rebase-20260511-0136/<name>
```

## 🔧 Worktree 매핑

```
~/dev/jcode       = deploy/m9-m10  ← 안정/검증/배포 path
~/dev/jcode-m11   = patch/lifecycle-hooks  ← M11 작업 path
```

## 🛠 사용자 환경 / 약속

- 한국어 대화.
- commit 분리: `fix(mXX):`, `test(mXX):`, `docs:` 별개 commit.
- author: `lazydino <lazydino@users.noreply.github.com>`
  (`git -c user.name=lazydino -c user.email=lazydino@users.noreply.github.com`)
- patch branch 들은 `master` 베이스 (deploy 위에 stack 안 함).
- 통합 시 deploy/m9-m10 으로 cherry-pick 또는 manual port.
- release 빌드 → atomic 배포 패턴:
  ```
  install -m 0755 target/release/jcode "$HOME/.jcode/builds/versions/lazydino-${COMMIT}/jcode"
  ln -sfn "$HOME/.jcode/builds/versions/lazydino-${COMMIT}/jcode" \
      "$HOME/.jcode/builds/current/jcode"
  ln -sfn "$HOME/.jcode/builds/versions/lazydino-${COMMIT}/jcode" \
      "$HOME/.jcode/builds/stable/jcode"
  install -m 0755 target/release/jcode "$HOME/.local/bin/jcode"
  ```
- 절대 떠 있는 server kill 금지. 사용자 본인이 close+reopen.
- subagent spawn 가능. 단, **같은 round 에 두 번 spawn 시 두번째가
  background 로 deferred** 되는 jcode 버그 있음 (잠재 M22). 한 round
  에 하나씩 spawn 권장.

## 🐛 잠재 milestone 후보 (등록 안 됨)

- **M22 (subagent same-round spawn defer)**: 한 turn 에서 두 번째
  subagent spawn 이 background 로 밀림. workaround: 한 round 당
  한 subagent.
- **M23 (build artifact cleanup)**: `~/.jcode/builds/versions/` 가
  계속 누적됨. retention policy 없음. 디스크 차면 등록.

## 📚 핵심 파일 빠른 reference

```
LAZYDINO_MILESTONES.md           # 마일스톤 마스터 (M1~M20 등록)
LAZYDINO_LIVE_VERIFICATION_*.md  # 라이브 검증 plan
LAZYDINO_MAINTENANCE.md          # 운영 노트
LAZYDINO_TEST_PLAN.md            # 테스트 plan

docs/lazydino/
├── analysis/
│   ├── M11-stages-1-2.md       # M11 분석 (먼저 진행할 부분)
│   ├── M11-stages-3-5.md       # M11 분석 (나중)
│   └── M19-config-hot-reload.md # M19 결정 배경
└── sessions/
    ├── 2026-05-11-m19-impl.md          # 이번 세션 중간 노트
    ├── 2026-05-11-m19-m20-deployed.md  # 이번 세션 final 노트 ★
    └── HANDOFF-2026-05-11.md            # 이 파일
```

## 🚦 첫 응답에서 사용자에게 보고할 내용

1. 직전 세션 요약: M19/M20 배포 완료 (`f50584a3` tip, binary
   `lazydino-da4fe48f`). M21 (upstream rebase) OPEN 으로 등록.
   백업 tag 48개 확보.
2. 사용자 close+reopen 했는지 확인 — 안 했으면 그것부터 안내.
3. PAT `workflow` scope 갱신 했는지 확인 — 했으면 M21 진행 가능.
4. 우선순위: **M21 부터** 권장 (그래야 다음 fix 들이 path mismatch
   없이 깔끔하게 push 됨). 사용자가 M21 미루고 M11/M18 부터 하길
   원하면 그것도 가능하지만, 누적되면 M21 작업량만 커짐.

---

## 📜 직전 세션의 핵심 commit (참조용)

```
deploy/m9-m10:
  f50584a3 docs(m21): register upstream rebase milestone + per-patch backup tag protocol
  4dc0a114 docs(m19,m20): mark DONE, write final session note + next-session handoff
  da4fe48f test(m19): cover hot-reload, last-good fallback and recovery
  96445a80 fix(m19): hot-reload config on mtime change
  e6f98968 test(m20): cover resolve_timeout_ms precedence and BashToolConfig clamping
  28b524ba fix(m20): make bash tool timeout configurable, raise default to 5min cap to 20min
  e51fa9f6 docs: register M19 (DONE, integration pending) + M20 (bash 2min timeout)

patch/config-hot-reload:
  bdf63f5c test(m19): cover hot-reload, last-good fallback and recovery
  93282b44 fix(m19): hot-reload config on mtime change

patch/bash-tool-timeout:
  10db6a96 test(m20): cover resolve_timeout_ms precedence and BashToolConfig clamping
  11edbb91 fix(m20): make bash tool timeout configurable, raise default to 5min cap to 20min

backup tag (rollback 용):
  backup/pre-upstream-rebase-20260511-0136/deploy-m9-m10
  backup/pre-upstream-rebase-20260511-0136/<patch-name>   # × 47개
```

**"네, 위 상태 파악했습니다. close+reopen 하셨고 PAT workflow scope
갱신하셨나요? 됐으면 M21 (upstream rebase + dedupe) 부터 시작하겠습니다.
아직이면 우선 M11 또는 M18 로 진행할까요?"** 같은 톤으로 시작하면
자연스럽습니다.
