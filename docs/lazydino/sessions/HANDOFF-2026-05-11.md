# HANDOFF — 2026-05-11 → 다음 세션 (Round 6 종료 시점)

> 다음 세션 시작 시 **이 파일 전체를 복사해서 첫 메시지로 붙여넣으세요.**
> 한국어로 진행. fix/docs/test commit 분리. lazydino author identity.

---

## 📌 즉시 실행해서 상태 검증 (5초)

```bash
cd /home/lazydino/dev/jcode && \
git log --oneline deploy/m9-m10 -5 && \
echo "---" && \
ls -la ~/.local/bin/jcode && \
readlink -f ~/.local/bin/jcode && \
echo "---" && \
git rev-list --count fork/master..origin/master && \
git ls-remote --heads fork | wc -l
```

**기대값:**
- 최상단: `dc4bebb5 fix(m21): land fork push with PAT askpass helper`
- 그 아래: `c00c2a39 docs(milestones): clean stale M19 OPEN ...` →
  `acae64cf docs(handoff): reflect M18 done ...` →
  `8dd18a72 docs(m18): mark DONE ...` → `fe7eb87e test(m18): ...`
- `~/.local/bin/jcode` 가 94 MiB binary, **lazydino-dc4bebb5** (M9 +
  M10 + M18 + M19 + M20 포함)
- `fork/master..origin/master = 0` (fork 가 upstream 과 sync)
- fork ref 51 개 (master + deploy/m9-m10 + 48 patch + ...)
- backup tag: 48 (upstream rebase) + 2 (m18 integration) + 1 (fork push)
  = 총 51 개

## ✅ 직전 세션 (2026-05-11, Round 6) 완료 사항

이번 라운드는 **fork 완전 동기화 + 워크플로우 자동화** 가 핵심.

1. **M18 통합 마무리** — fix/test 분리 commit 으로 정리하고
   deploy/m9-m10 에 cherry-pick (`4531dc5d`/`fe7eb87e`), binary
   배포 (`lazydino-dc4bebb5`) 까지 완료. 이전 라운드의 미완 봉합.
2. **M21 (upstream rebase + dedupe) ✅ DONE** — `~/dev/medivance/.env.local`
   의 `GH_TOKEN` (scope: repo+workflow, user: lazy-dinosaur) 으로
   askpass push.
   - `fork/master` ← `origin/master` 226 commit fast-forward
   - `deploy/m9-m10` + 48 patch branch 모두 fork 에 push (총 51 ref)
   - dedupe rebase 불필요 (patch 들이 이미 origin/master 기준)
   - 충돌 0 건, force-push 0 건
3. **`/.env` + `scripts/fork-push.sh` 인프라** — local `.env` (0600,
   gitignored) 에 GH_TOKEN 보관. 헬퍼 스크립트는 askpass 통해 자동
   인증. 다음 push 부터 `./scripts/fork-push.sh deploy/m9-m10` 한 줄.
4. **문서 청소** — `LAZYDINO_MILESTONES.md` 의 stale M19 OPEN row
   삭제, M22 (subagent same-round spawn defer) + M23 (build artifact
   cleanup) 를 latent → 정식 등록.
5. **백업 tag 51개 전부 보존**:
   - 48 × `backup/pre-upstream-rebase-20260511-0136/*`
   - 2 × `backup/pre-m18-integration-20260511-0733/*`
   - 1 × `backup/pre-fork-push-20260511-0743/deploy-m9-m10`

## 🟡 사용자 액션 대기

**현재 떠 있는 jcode server 들은 옛 binary 메모리에 들고 있을 수 있음.**
새 fix (M18 / M19 / M20) 효과 보려면 **TUI close + reopen** 필요.
다음 세션 시작 시 사용자에게 확인할 것:

- 이미 close+reopen 했는지? → 했으면 라이브 검증 가능
- 안 했으면? → 검증 전에 close+reopen 권유

## 📋 다음 작업 후보 (우선순위, M21 DONE 후 재정렬)

### 🎯 다음 세션 시작 시 추천 흐름

1. 사용자에게 "M11 진행할래 M17 할래?" 묻기 (둘 다 High)
2. 선택된 milestone 카드 읽기: `docs/lazydino/milestones/M{XX}.md`
3. 카드의 "환경" / "시작점" / "작업 분해" 그대로 따라가면 됨

### 마일스톤 카드 (다른 세션 픽업용, Round 6 신규)

**모든 OPEN milestone 이 self-contained 카드로 정리됨**:
`docs/lazydino/milestones/README.md` 에 index, 각 `M{XX}.md` 에 worktree
/ 코드 위치 / 작업 분해 / 검증 / 회귀 위험 / 추천 시작 순서.

| 순위 | 마일스톤 | 우선도 | 카드 |
|---|---|---|---|
| **1** | **M11** lifecycle hook decision parsing | **High** (framework 게이트) | [M11.md](../milestones/M11.md) |
| **2** | **M17** live turn handoff to swarm | **High** (워크플로우) | [M17.md](../milestones/M17.md) |
| 3 | M16 Anthropic OAuth tool 광고 통일 | Medium-High | [M16.md](../milestones/M16.md) |
| 4 | M2 swarm 버그 (issue #76) | Medium-High | [M2.md](../milestones/M2.md) |
| 5 | M4 TUI interleave UX | Medium (BY-DESIGN) | [M4.md](../milestones/M4.md) |
| 6 | M15 외부 client 에 image data | Low-Medium | [M15.md](../milestones/M15.md) |
| 7 | M22 subagent same-round spawn | Low | [M22.md](../milestones/M22.md) |
| 8 | M23 build artifact cleanup | Low | [M23.md](../milestones/M23.md) |

**제안 시작점**: **M11** 부터 (분석 완료 + framework 게이트). 또는
사용자가 TUI 워크플로우 가치 더 크면 **M17** 부터 (A vs B1 결정 1회 + ~50줄).

## 🔒 백업 tag (rollback 용)

| Prefix | 개수 | 시점 |
|---|---|---|
| `backup/pre-upstream-rebase-20260511-0136/` | 48 | M21 시작 전 (Round 4) |
| `backup/pre-m18-integration-20260511-0733/` | 2 | M18 통합 직전 (Round 5) |
| `backup/pre-fork-push-20260511-0743/` | 1 | 첫 fork push 직전 (Round 5) |

rollback 예시:
```bash
git checkout -B deploy/m9-m10 \
  backup/pre-upstream-rebase-20260511-0136/deploy-m9-m10
git checkout -B patch/<name> \
  backup/pre-upstream-rebase-20260511-0136/<name>
```

## 🔧 Worktree 매핑

```
~/dev/jcode       = deploy/m9-m10  ← 안정/검증/배포 path (tip: dc4bebb5)
~/dev/jcode-m11   = patch/lifecycle-hooks  ← M11 작업 path (tip: 5d81c901)
~/dev/jcode-m18   = patch/sdk-history-images  ← M18 DONE (tip: 9051050b)
/tmp/jcode-d2c9b046  ← detached HEAD, 무시 가능
```

## 🛠 사용자 환경 / 약속 (cumulative)

- **언어**: 한국어 대화.
- **commit**: `fix(mXX):`, `test(mXX):`, `docs:` 별개. 한 commit 에
  fix+test 섞이면 split-and-recommit (M18 사례 참고).
- **author**: `lazydino <lazydino@users.noreply.github.com>`
  → `git -c user.name=lazydino -c user.email=lazydino@users.noreply.github.com commit ...`
- **patch 베이스**: 항상 clean `origin/master` (deploy 위에 stack 금지).
  통합은 cherry-pick 또는 manual port.
- **toolchain**: `cargo +nightly` 필수 (stable rustc 1.90 은 upstream
  AWS crates MSRV 1.91.1 막힘). 향후 stable 가능해질 때까지 nightly 유지.
- **release 빌드 → atomic 배포 패턴**:
  ```bash
  cargo +nightly build --release
  TIP=$(git rev-parse --short=8 deploy/m9-m10)
  install -m 0755 target/release/jcode \
    "$HOME/.jcode/builds/versions/lazydino-${TIP}/jcode"
  ln -sfn "$HOME/.jcode/builds/versions/lazydino-${TIP}/jcode" \
    "$HOME/.jcode/builds/current/jcode"
  ln -sfn "$HOME/.jcode/builds/versions/lazydino-${TIP}/jcode" \
    "$HOME/.jcode/builds/stable/jcode"
  install -m 0755 target/release/jcode "$HOME/.local/bin/jcode"
  ```
- **떠 있는 server 절대 kill 금지**. 사용자 본인이 close+reopen.
- **subagent**: round 당 1 개 spawn 권장 (M22 버그 회피).
- **remote**:
  - `origin = https://github.com/1jehuang/jcode.git` (upstream of fork)
  - `fork   = https://github.com/lazy-dinosaur/jcode.git` (개인 fork)
  - ❌ `upstream=sst/jcode` 절대 추가 금지 (잘못된 repo, fetch 실패)

## 🔑 GH_TOKEN / fork push 자동화 ★ Round 5 신규

- `/home/lazydino/dev/jcode/.env` (mode 0600, gitignored) 에 보관:
  ```
  GH_TOKEN=ghp_...
  GH_USERNAME=lazy-dinosaur
  ```
- 누락 시 복구: `~/dev/medivance/.env.local` 의 `GH_TOKEN` 복사.
- scope 확인:
  ```bash
  curl -sI -H "Authorization: token $GH_TOKEN" https://api.github.com/user \
    | grep -i "x-oauth-scopes"
  # 기대: repo, workflow
  ```
- 일상 push:
  ```bash
  ./scripts/fork-push.sh deploy/m9-m10           # 일반 fix 후
  ./scripts/fork-push.sh master                  # upstream 동기화
  ./scripts/fork-push.sh                         # 기본 ref 세트
  ./scripts/fork-push.sh deploy/m9-m10 patch/foo # 여러 ref
  ```

## 🐛 잠재 milestone 후보

이번 라운드까지 잠재 → 정식 등록 모두 흡수 완료 (M22, M23).
새로 발견한 잠재 항목이 있으면 이 섹션에 메모.

## 📚 핵심 파일 빠른 reference

```
LAZYDINO_MILESTONES.md           # 마스터 (M1~M23 등록, OPEN 14건)
LAZYDINO_MAINTENANCE.md          # 운영 노트 (.env / fork-push 절차 포함)
LAZYDINO_LIVE_VERIFICATION_*.md  # 라이브 검증 plan
LAZYDINO_TEST_PLAN.md            # 테스트 plan
scripts/fork-push.sh             # fork push 헬퍼 ★ NEW
.env                             # GH_TOKEN (gitignored) ★ NEW

docs/lazydino/
├── analysis/
│   ├── M11-stages-1-2.md       # M11 분석 (다음 세션 시작점)
│   ├── M11-stages-3-5.md       # M11 분석 (후속)
│   └── M19-config-hot-reload.md
└── sessions/
    ├── 2026-05-11-m19-impl.md
    ├── 2026-05-11-m19-m20-deployed.md
    └── HANDOFF-2026-05-11.md   # 이 파일
```

## 🚦 첫 응답에서 사용자에게 보고할 내용

1. **상태 요약**: 직전 라운드 M21 DONE (fork 226 commit + 48 patch
   sync 완료), binary `lazydino-dc4bebb5` 배포 됨. M18/M19/M20 효과는
   close+reopen 후 검증 가능.
2. **확인**: 사용자가 TUI close+reopen 했는지?
3. **시작점 제안**: **M11 (분석 완료, framework 게이트 영향)** 또는
   **M17 (워크플로우 직접 영향, A vs B1 결정 1회)** 중 어느 쪽 진행할지.
4. 사용자가 다른 milestone (M15/M16) 또는 새 요구 제시하면 거기 따라.

---

## 📜 직전 세션 핵심 commit (참조용)

```
deploy/m9-m10 (tip dc4bebb5):
  dc4bebb5 fix(m21): land fork push with PAT askpass helper
  c00c2a39 docs(milestones): clean stale M19 OPEN entry, promote M22+M23
  acae64cf docs(handoff): reflect M18 done + lazydino-dc4bebb5 binary
  8dd18a72 docs(m18): mark DONE after integration + binary deploy
  fe7eb87e test(m18): cover split_history_event image preservation
  4531dc5d fix(m18): preserve images in SDK history API
  99c1d55e docs(m21): record remote correction and PAT blocker
  cd5d0ec9 docs(m18): record SDK history image patch
  ...
  da4fe48f test(m19) / 96445a80 fix(m19)
  e6f98968 test(m20) / 28b524ba fix(m20)

patch branches (모두 fork 에 push 완료, 48개):
  patch/sdk-history-images   (M18 — DONE)
  patch/config-hot-reload    (M19 — DONE)
  patch/bash-tool-timeout    (M20 — DONE)
  patch/lifecycle-hooks      (M11 — WIP, 다음 세션 시작점)
  patch/agent-profiles, patch/altb-*, patch/swarm-*, ...

binary:
  ~/.local/bin/jcode -> 94 MiB lazydino-dc4bebb5
  M9 + M10 + M18 + M19 + M20 통합본

fork (lazy-dinosaur/jcode):
  master (= origin/master, 226 commit FF 됨)
  deploy/m9-m10 (= local deploy/m9-m10)
  patch/* × 48
  총 51 ref, fork == origin sync 상태
```

**첫 응답 톤 예시:**
> "네, 상태 파악했습니다. M21 DONE 으로 fork 가 upstream + 우리 99 commit
> 까지 완전 sync 됐고, binary `lazydino-dc4bebb5` 가 ~/.local/bin/jcode
> 에 떠있습니다. TUI close+reopen 하셨나요?
>
> 다음 작업은 **M11 (lifecycle hook decision parsing, 분석 완료)** 또는
> **M17 (live turn handoff to swarm, A/B1 결정 필요)** 중 어느 쪽으로
> 진행할까요? 둘 다 High priority 입니다."
