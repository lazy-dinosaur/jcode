# M43 — Subagent 에서 `bg` 도구 접근 / system reminder 안내 불일치

**Priority**: Medium (UX/시간낭비, workaround 있음)
**Status**: 🔴 OPEN, 진단 미수행 (라이브 1회 관찰)
**Discovered**: 2026-05-13 00:14 KST 메인 세션 라이브 (M40 작업 중 사이드 발견)

## 한 줄 요약

메인 세션이 `bg action="output" task_id=...` 으로 background task
결과를 fetch 하라는 system reminder 를 받았는데, 그걸 **subagent 에
위임** 했더니 subagent 가 "내 도구 목록에 bg 없음" 으로 실패. 결국
메인이 직접 호출해야 한다는 걸 깨닫는 데 추가 round 소비 (codesearch,
bash find 등 헛수고 후).

## 사용자 보고

> "system reminder 에 bg action="output" task_id="503346c9i1"
> 명시돼 있는데, 이 tool 은 메인 세션에서만 호출 가능한 형태야.
> 직접 호출하자."
> "sub-agent 에는 bg 도구가 없네. 메인 세션에서 직접 호출해야 해."
> "이것도 버그같은데 맞아??"

## 코드 검증 (초기)

- `src/tool/mod.rs:172`: `BgTool` 가 base tool 로 등록.
- `src/tool/task.rs:416-435`: subagent 가 받는 tool set 은
  `self.registry.tool_names()` 전체 minus `{subagent, task, todo*}`
  (recursion 방지). **`bg` 는 명시적으로 안 빠짐** → subagent 에서도
  보여야 한다는 의미.

즉 **두 가지 가능성**:

### 가설 A: 실제로는 subagent 에서 보임, 메인이 추측 오류

- 메인이 "subagent 는 bg 없어" 라고 단정한 게 실측 아닌 추측일 수도.
- 검증: subagent (e.g. `subagent_type=quick`) 의 system prompt /
  tool list 직접 dump 해서 `bg` 포함 여부 확인.

### 가설 B: bg 가 register 되어 있어도 session-scope 때문에 실패

- `task_id=503346c9i1` 는 **메인 세션 (ctx.session_id) 의 task**.
- subagent 는 자기 session 이 따로 (`Session::create` ).
- `background` 모듈이 session-scoped 이면 subagent 에서 같은 task_id
  로 output fetch 시도 → "task not found" 류 에러.
- 그게 "subagent 에 bg 가 없다" 처럼 보였을 수 있음.

## 핵심 파일

- `src/tool/bg.rs` (BgTool 구현)
- `src/tool/mod.rs:172` (등록)
- `src/tool/task.rs:380-435` (subagent 의 allowed tools 결정)
- `src/background/` (background task store, scoping)

## 추가 관찰 (같은 라이브)

- `bash $ ... cargo test ...` 가 **300000ms timeout** 으로 실패
  (task_id `718519zl72`). bash default timeout 5분 ��� release 빌드
  + 테스트 합치면 자주 넘김. M40 빌드 결과 fetch 자체는 별개로 OK
  (이미 5m 04s 에 끝났음). M40 fix 영향 0.
  - **개선 후보**: lazydino 운영 원칙 (MEMORY) 에 "long cargo build
    은 `nohup ... & disown` + 폴링" 으로 이미 명시. agent 가 그
    원칙 따르면 회피 가능.

## 진단 plan (다음 세션)

1. 메인 세션에서 `subagent_type=quick` 같은 가벼운 subagent spawn 후
   subagent 에게 "당신 tool list 를 dump 하라" 시키기.
   - `bg` 가 보이면 **가설 B** 확정 (session-scope 문제)
   - 안 보이면 **가설 A** 새 발견 (tool filtering 다른 경로 존재)
2. `background` 모듈의 task lookup 함수에서 session_id 필터링
   조건 확인.
3. fix 방향:
   - 가설 A: tool filtering 수정 or system reminder 가 "이건 메인
     세션 전용" 명시
   - 가설 B: `bg action="output"` 만 cross-session read-only 허용
     (작업 위임이 자연스럽게 됨) 또는 system reminder 에 "subagent
     에 위임하지 말고 직접 호출" 명시

## Constraints

- 활성 server 절대 kill 금지
- `patch/m43-*` branch, `fix(m43):` commit prefix
- workaround (사용자 본인이 "메인에서 직접 호출하자" 라고 판단) 가
  있으니 우선순위 Medium

## 관련 milestone

- **M22** (subagent same-round defer): subagent 실행 모델 제약 관련.
  M43 도 subagent 의 tool scope 관련 제약.
- **M41** (이미 DONE): server-initiated turn redraw.
- **M40** (진행 중): leading-space slash, image attach, opus 1m.
