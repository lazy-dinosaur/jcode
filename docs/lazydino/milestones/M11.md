# M11 — Lifecycle Hook Decision Parsing

**Priority**: High (framework 게이트 강제력)
**Status**: 🔴 OPEN, 분석 완료, worktree 준비됨

## 한 줄 요약

Lifecycle hook (`response.completed`, `session.stop`) 이 stdout JSON
의 `decision.action` 을 파싱하지 않음. 그래서 tool hook 의 `deny` 패턴
처럼 "AI 다음 응답에 reason 주입" 이 불가능. `tool.execute.before` 와
비대칭.

## 환경

```bash
cd /home/lazydino/dev/jcode-m11   # worktree 이미 있음
git status
# On branch patch/lifecycle-hooks (tip ~ 5d81c901)
git log --oneline -5
```

분석 문서:
- `docs/lazydino/analysis/M11-stages-1-2.md` — stages 1+2 상세
- `docs/lazydino/analysis/M11-stages-3-5.md` — stages 3-5 상세

## 핵심 파일

- `src/hooks.rs` — `run_blocking_hook`, `HookDecision` struct,
  `run_tool_hooks` (지금 tool hook 만 decision parsing)
- `src/hooks.rs:302-320` — lifecycle 분기에서 parsing 안 함 (root cause)
- `src/config/default_file.rs:281` — 한계 명시 주석 있음
- `src/agent/turn_loops.rs:867-898` — Err(e) → ToolResult error 변환
  (Stage 2 path)
- `src/agent/turn_streaming_*.rs` — 동일 패턴

## 5 단계 patch

### Stage 1: lifecycle decision parsing (~40-70 줄)
- `run_blocking_lifecycle_hook` 에 stdout JSON parsing 추가
- `HookDecision { action, reason }` 그대로 재사용
- `action="deny"` 이면 lifecycle 진행 멈춤 + reason 전달
- 회귀 테스트: parser 엣지케이스 (invalid JSON, unknown action,
  case sensitivity, multi-line stdout)

### Stage 2: reason → next-turn system reminder inject (~50-100 줄)
- 현재 turn 이 `response.completed`/`session.stop` 단계 → 이미 break 직전
- 새 turn 시작 메커니즘 필요: 다음 user turn 의 system prompt 에
  reason 을 hidden reminder 로 삽입
- 후보 위치: `src/agent/turn_loops.rs` 의 turn 시작 hook
- 또는 message queue 에 `{role:"system", content:"<hook reason>"}` 주입

### Stage 3: `stop_hook_active` 무한루프 방지
- N회 연속 deny 시 hard-stop
- 카운터를 session state 에 저장 (예: `stop_hook_consecutive_deny: u8`)
- threshold (예: 3) 넘으면 reason 무시하고 진짜 stop
- 회귀 테스트: stop hook 이 3번 연속 deny → 4번째에 강제 stop

### Stage 4: `session.stop` vs `client.disconnect` 의미 분리 (M12 후속)
- 사용자 명시 stop 과 client 비정상 disconnect 를 분리
- hook 이 둘 다에 같은 동작하지 않게

### Stage 5: payload context 보강 ★ 핵심
- 현재 hook script 는 빈 stdin 으로 호출 → 검증 불가능
- 추가 필드: `last_user_message`, `recent_tool_calls`,
  `turn_count`, `session_age_seconds`
- hook script 가 "마지막 메시지가 X 면 deny" 같은 정책 작성 가능
- JSON payload schema 정의 + 회귀 테스트

## 시작 순서

1. **stages 1+2 부터.** 작업량 합쳐 ~100-170 줄, ~2-3h
2. stages 1+2 완료되면 별도 commit (`fix(m11):` + `test(m11):` 분리)
3. cherry-pick to `deploy/m9-m10`
4. 빌드 + 배포 + push
5. 사용자에게 close+reopen 안내, hook script 로 라이브 검증
6. 그 다음 stage 3, 4, 5 순차 진행 (각각 별도 PR-able commit)

## 사용자 검증 시나리오

```bash
# ~/.jcode/config.toml 에 추가
[[hooks]]
event = "session.stop"
blocking = true
command = '''
echo '{"action":"deny","reason":"꼭 git push 하고 stop 해주세요"}'
'''

# jcode 시작 → /reload 또는 자연스럽게 turn 끝나면
# 다음 응답에 reason 이 system reminder 로 나와야 함
```

## 회귀 위험

- Stage 1: 기존 lifecycle hook 사용자가 "stdout 무시되던 동작" 에
  의존 중이면 surprise. mitigation: 빈 stdout 은 여전히 allow 처리.
- Stage 3: stop hook 이 reason 으로 사용자 행동 강제 → 너무 강하면
  unstoppable AI 됨. counter + hard-stop 으로 escape 보장.

## 분석 출처

- `docs/lazydino/analysis/M11-stages-1-2.md` (이미 작성됨)
- `docs/lazydino/analysis/M11-stages-3-5.md` (이미 작성됨)
- 참조: timsquad/claude-code 의 stop hook 패턴
