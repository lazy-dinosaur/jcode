# M22 — Subagent Same-Round Spawn Defer

**Priority**: Low (workaround 있음)
**Status**: 🔴 OPEN, 미진단

## 한 줄 요약

Main session 한 turn 안에서 subagent (`task`/`subagent` tool) 를
두 번 spawn 하면 두 번째가 즉시 실행 안 되고 background 로 deferred.
의도된 직렬화인지, jcode 버그인지 미확정.

## Workaround

**Round 당 1 spawn 만.** 현재 모든 세션이 이 규칙 따르고 있음.

## 환경

```bash
cd /home/lazydino/dev/jcode
git worktree add ../jcode-m22 -b patch/subagent-same-round-spawn origin/master
cd ../jcode-m22
```

## 진단부터

### 추정 위치

```bash
grep -rn "TurnState\|turn_state" src/ --include='*.rs' | head -10
grep -rn "subagent.*spawn\|spawn.*subagent" src/ --include='*.rs' | head -10
ls src/swarm/dispatch* 2>/dev/null
```

### 두 가능성

**(a) Mutex<TurnState> 가 turn 종료까지 잡힘**
- 같은 turn 의 두 번째 spawn 이 lock 못 잡고 queue 로 밀림
- fix: subagent spawn 은 lock free 또는 short-lived lock

**(b) 의도된 single-spawn-per-turn 정책**
- 그렇다면 docs 갱신만 (코드 변경 0)

### 진단 plan

1. `subagent` tool dispatch path 에 trace log 추가 (~20줄)
2. 두 번째 spawn 시점에 state inspect (lock holder, queue 상태)
3. 결과:
   - (a) 라면 fix 시도
   - (b) 라면 limitation 으로 docs 에 명시

## 작업 분해

1. Trace log 추가 + 재현 (~1h)
2. 결과 분석 (~30분)
3. (a) fix 시도 또는 (b) docs 작성

## 사용자 검증 시나리오

```text
사용자: "두 subagent 동시에 spawn 해줘 (1: code search, 2: doc lookup)"
[trace log 확인]
- (a) 라면: spawn 1 lock 잡고 spawn 2 wait → fix 가능
- (b) 라면: 정책. docs 에 명시.
```

## 회귀 위험

낮음. workaround 있으니 fix 안 해도 사용자 영향 없음. 잘못 fix 하면
turn 안에서 동시 spawn → race condition 더 위험할 수 있음.

## 추천

**M11 / M17 / M16 다 끝나고 마지막에**. workaround (round 당 1) 가
실용적으로 작동 중.
