# B11 upstream catch-up decisions — 2026-05-24

Range audited: `c09268f2..e4fd23f5` (22 upstream commits after B10).

## Summary

- `PORT_UPSTREAM`: 2
- `HYBRID_REFACTOR`: 4
- `KEEP_OURS`: 16
- `SKIP_UPSTREAM`: 0
- `NEEDS_REVIEW`: 0

B11 is mostly test stabilization, desktop UI, slash-suggestion UI, and agent memory handling. This pass ports low-risk runtime/build fixes and defers large UI and memory config surface changes.

## Decisions

| Upstream commit | Subject | Decision | Local handling |
| --- | --- | --- | --- |
| `6cfc08bb` | test: lock env-sensitive browser and cli tests | `KEEP_OURS` | Test stabilization only; local env-lock coverage already differs. |
| `f7284865` | test: use global env lock for jcode home tests | `KEEP_OURS` | Test-only. Deferred. |
| `ff0b9126` | test: use global env lock in remaining jcode home tests | `KEEP_OURS` | Test-only. Deferred. |
| `31dcc17c` | fix(tui): keep catchup picker open on enter | `PORT_UPSTREAM` | Ported CatchUp mode handling for `SelectedInCurrentTerminal`. Validated with catchup tests. |
| `5dc2646b` | test(tui): account for initial session context | `KEEP_OURS` | Test-only expectation update. |
| `4d9c6f38` | test(tui): stabilize native scroll ghost repro | `KEEP_OURS` | Test-only, scroll model deferred. |
| `4971852a` | test(tui): wait for async git status results | `KEEP_OURS` | Test-only. |
| `0080519b` | test(tui): start compacted history lazy test hidden | `KEEP_OURS` | Test-only. |
| `9643d882` | test(tui): expect simplified model picker prehydration | `KEEP_OURS` | Test-only expectation update. |
| `8b23cb41` | test(tui): expect smooth side panel mouse pan | `KEEP_OURS` | Test-only, scroll/pan behavior deferred. |
| `6373ff52` | test(tui): expect smooth overlay mouse scroll | `KEEP_OURS` | Test-only. |
| `9b8f4dd5` | test(tui): use materialized provider view in paste tests | `KEEP_OURS` | Test-only. |
| `607bd662` | test(tui): expect immediate side pane mouse scroll drain | `KEEP_OURS` | Test-only. |
| `f0f37517` | build: harden low-memory selfdev builds | `PORT_UPSTREAM` | Ported `SCCACHE_DISABLE` handling and low-memory selfdev incremental/codegen-unit defaults. Validated with `bash -n` and selfdev build later. |
| `442c62fa` | Redesign desktop inline widget chrome | `KEEP_OURS` | Desktop-only. Deferred. |
| `d69fb2a5` | docs: define resume picker behavior | `KEEP_OURS` | Docs/default config only; local resume behavior already customized. |
| `c3f63b38` | feat(desktop): promote workspace sessions in place | `KEEP_OURS` | Desktop-only. Deferred. |
| `5c835364` | Render slash command suggestions vertically | `HYBRID_REFACTOR` | Large TUI UI change. Deferred to slash-suggestion pass. |
| `c2974ee8` | Add selectable slash command suggestions | `HYBRID_REFACTOR` | Large TUI input interaction change. Deferred. |
| `e9a758e8` | Fix slash suggestion navigation edge cases | `HYBRID_REFACTOR` | Depends on selectable suggestion model. Deferred. |
| `da39d0e0` | Show eight slash command suggestions | `KEEP_OURS` | Depends on deferred suggestion layout. |
| `e4fd23f5` | feat(agent): improve memory injection handling | `HYBRID_REFACTOR` | Local tree already has memory record/dedup and empty-after-tool continuation handling. Upstream adds config surface for persisted memory injection and broader streaming changes; defer to a focused memory-injection pass. |

## Validation performed

- `cargo fmt --check`
- `cargo check -q -p jcode`
- `cargo test -q -p jcode catchup -- --nocapture`
- `bash -n scripts/dev_cargo.sh`

## Notes

B11 slash suggestion commits are grouped for a future TUI input UX pass. Memory injection handling is partially already present locally and should be reconciled separately to avoid changing history persistence semantics during catch-up.
