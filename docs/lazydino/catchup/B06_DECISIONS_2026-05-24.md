# B06 upstream catch-up decisions — 2026-05-24

Range audited: `5b52a370..95087d57` (20 upstream commits after B05).

## Summary

- `PORT_UPSTREAM`: 3
- `HYBRID_REFACTOR`: 2
- `KEEP_OURS`: 14
- `SKIP_UPSTREAM`: 1
- `NEEDS_REVIEW`: 0

B06 is mostly desktop rendering/workspace work. This pass ports non-desktop runtime safety and selfdev fixes, while deferring desktop-only renderer/window/workspace changes to a dedicated desktop catch-up block.

## Decisions

| Upstream commit | Subject | Decision | Local handling |
| --- | --- | --- | --- |
| `828a3712` | Synchronize spinner-only TUI redraws | `PORT_UPSTREAM` | Ported spinner interval helper with `MissedTickBehavior::Skip` and reset after full redraws/input-triggered redraws. |
| `9ff75685` | Fix desktop window title | `KEEP_OURS` | Desktop-only. Deferred to desktop catch-up. |
| `872995fa` | Align desktop tool call summaries | `KEEP_OURS` | Desktop-only. Deferred to desktop catch-up. |
| `5f2fa8ad` | Style desktop inline code pills | `KEEP_OURS` | Desktop-only markdown rendering. Deferred to desktop catch-up. |
| `ab0e3234` | Avoid resume hint panic on closed stderr | `PORT_UPSTREAM` | Ported `write_session_resume_hint` returning `io::Result<()>` and ignored errors in signal path. Added closed-writer regression test. |
| `6b64d173` | Improve desktop error diagnostics | `KEEP_OURS` | Desktop-only diagnostics and server IO logging. Deferred to desktop catch-up. |
| `e9cd721d` | Improve desktop markdown rendering | `KEEP_OURS` | Desktop-only rendering. Deferred to desktop catch-up. |
| `cd5dfeb4` | Test desktop markdown render geometry | `KEEP_OURS` | Desktop-only tests. Deferred to desktop catch-up. |
| `15703501` | style: simplify remote UI activity arm | `KEEP_OURS` | Style-only. Local B05 remote event handling already diverged with redraw-return filtering. No behavior gap. |
| `a4088a70` | feat(desktop): refine workspace and single session interactions | `KEEP_OURS` | Desktop-only workspace/session UX. Deferred to desktop catch-up. |
| `ec071e19` | test(desktop): cover streaming text fade | `KEEP_OURS` | Desktop-only test. Deferred to desktop catch-up. |
| `77eb385d` | fix(selfdev): validate desktop build artifact | `PORT_UPSTREAM` | Ported desktop-only selfdev build validation so desktop builds smoke-test `target/selfdev/jcode-desktop` instead of publishing the TUI binary. |
| `69382349` | fix(desktop): make activity spinner visible | `KEEP_OURS` | Desktop-only rendering plus shared helper churn. Deferred to desktop catch-up. |
| `7c884082` | feat(desktop): integrate activity cue with streaming text | `KEEP_OURS` | Desktop-only rendering experiment. Deferred to desktop catch-up. |
| `aafae3ca` | Handle cancel before server ack backpressure | `HYBRID_REFACTOR` | Local tree already has `prefire_cancel_request`, which requests cancellation before taking the shared writer lock for Ack. Existing regression `prefire_cancel_request_sets_turn_signal_before_ack_path` passes. No additional behavior port needed in B06. |
| `1180d7c0` | feat(desktop): blend activity cue with text streaming | `KEEP_OURS` | Desktop-only experiment. Deferred to desktop catch-up. |
| `35faaec4` | Revert desktop activity cue blending | `SKIP_UPSTREAM` | Revert of deferred desktop-only experiment. No local change needed. |
| `4d85a64b` | Add comprehensive interrupt logging | `HYBRID_REFACTOR` | Large cross-cutting logging patch. Local tree already has cancellation prefire diagnostics and remote queue recovery changes in a different structure. Kept out of B06 to avoid high-churn logging behavior changes; revisit if debugging interrupt latency. |
| `e18a7349` | fix(desktop): render markdown inline spans cleanly | `KEEP_OURS` | Desktop-only rendering. Deferred to desktop catch-up. |
| `95087d57` | fix(desktop): render whitespace inline code pills | `KEEP_OURS` | Desktop-only rendering. Deferred to desktop catch-up. |

## Validation performed

- `cargo fmt`
- `cargo check -q -p jcode`
- `cargo test -q -p jcode session_resume_hint_writer_reports_closed_stderr_without_panicking -- --nocapture`
- `cargo test -q -p jcode test_session_recovery_message_format -- --nocapture`
- `cargo test -q -p jcode prefire_cancel_request_sets_turn_signal_before_ack_path -- --nocapture`

## Notes

B06 desktop-only commits are intentionally deferred as a group. Porting them piecemeal would mix renderer/workspace architecture changes with unrelated TUI/server runtime fixes. The current B06 code commit focuses on regressions that affect TUI stability, selfdev validation, and terminal signal cleanup.
