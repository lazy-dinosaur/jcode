# B03 upstream catchup decisions — 2026-05-24

Range: `c06d52e9..9a356287`

Rule: no blind cherry-pick. Decisions below are 3-way intent review against the LazyDino custom stack.

## Ported / hybrid ported

- `5cf30c58 Fix first batch subcall token badge`
  - Decision: `PORT_UPSTREAM`
  - Reason: upstream fixed a real parser edge case where `[tool timing ...] --- [1] ...` hides the first batch subcall token badge. LazyDino batch rendering is preserved.
  - Local commit: `4aa6893e Catch up upstream B03 TUI resume fixes`

- `0ddf16c7 Optimize resume session picker load`
  - Decision: `PORT_UPSTREAM`
  - Reason: avoids eager external transcript reads during `/resume` initial load while preserving LazyDino lazy transcript search through preview/query paths.
  - Local commit: `4aa6893e Catch up upstream B03 TUI resume fixes`

- `972cab06 Fix interrupt control for busy reconnects`
  - Decision: `HYBRID_REFACTOR`
  - Reason: upstream intent is correct, but LazyDino already has `TurnControl`, soft interrupt queues, and server-level registries. Ported lock-free refresh fallback using `SessionTurnControls` and added pre-stream cancel handling without replacing LazyDino interrupt semantics.
  - Local commit: `e3e00e1a Catch up upstream B03 busy reconnect interrupt fix`

- `8a0738f0 Restore only relevant crashed session group`
  - Decision: `HYBRID_REFACTOR`
  - Reason: upstream selected-group restore prevents stale crash restoration. LazyDino active-pids fast path and recovery metadata are preserved, with picker/CLI/inline restore routed to selected group IDs.
  - Local commit: `2f4d47e8 Catch up upstream B03 crash group restore`

- `5ee0b3d0 Guard oversized pasted submissions` and `58d30170 Increase oversized paste guard`
  - Decision: `PORT_UPSTREAM`
  - Local commit: `089019e9 Catch up upstream oversized paste guard`

- `b9920268 Mark direct provider fallback model routes`
  - Decision: `PORT_UPSTREAM`
  - Local commit: `4934cd44 Catch up upstream provider fallback routes`

- `aa07b11c Normalize non-object streamed tool arguments`
  - Decision: `PORT_UPSTREAM`
  - Local commit: `7ef949df Catch up upstream streamed tool argument normalization`

- `0e436d49 Improve resume picker key hints`
  - Decision: `PORT_UPSTREAM`
  - Local commit: `922d58f4 Catch up upstream resume picker key hints`

- `9a356287 Show live direct provider catalogs in model picker`
  - Decision: `HYBRID_REFACTOR`
  - Reason: reconciled with LazyDino Antigravity/Kimi/custom provider catalog routing instead of replacing route semantics.
  - Local commit: `08428467 Catch up upstream provider catalog updates`

## Kept / deferred

- Desktop rendering wave: `c06d52e9`, `87e56f2a`, `bd6b685e`, `76e4039c`, `a3c0e12b`, `1292c70c`, `d8a05328`
  - Decision: `KEEP_OURS`
  - Reason: LazyDino desktop already diverged substantially with handwritten welcome hero, runtime mask, transcript cards, inline widget layout, and richer model picker/filter behavior. Upstream patches are not safely superior as-is and would overwrite custom UX. Desktop crate tests pass on current implementation.

- Live verification evidence wave: `bf2be4df`, `3a947036`, `d1c8b181`
  - Decision: `NEEDS_REVIEW`
  - Reason: upstream adds a large `live_tests` evidence/checkpoint framework and changes auth-test/OpenRouter SSE behavior. LazyDino currently has a different auth-test report/probe structure. Do not port inside B03 without a separate design pass that maps upstream evidence ledger semantics onto LazyDino auth lifecycle and provider routing.

## Validation

- `cargo fmt --check`
- `cargo check -q -p jcode`
- `cargo check -q -p jcode-desktop`
- `cargo test -q -p jcode auth_test -- --nocapture`
- `cargo test -q -p jcode openrouter_sse -- --nocapture`
- focused tests listed in individual local commit messages
