# B04 upstream catch-up decisions — 2026-05-24

Range audited: `879f95ad..4ef3607e` inclusive boundary commit `879f95ad` plus 19 follow-ups.

## Summary

- `PORT_UPSTREAM`: 10
- `HYBRID_REFACTOR`: 4
- `KEEP_OURS`: 5
- `SKIP_UPSTREAM`: 1
- `NEEDS_REVIEW`: 0

B04 is closed for the current catch-up pass. Remaining upstream extraction-only commits were deliberately not cherry-picked because the Lazydino tree already has parallel crate/module extraction work in the same areas, and direct adoption would churn protected custom harness structure without changing behavior.

## Decisions

| Upstream commit | Subject | Decision | Local handling |
| --- | --- | --- | --- |
| `879f95ad` | Add client picker debug snapshot | `PORT_UPSTREAM` | Ported as `b713fe2a Catch up upstream B04 picker debug snapshot`. |
| `37e88eba` | Use live compatible model cache in remote picker | `HYBRID_REFACTOR` | Ported as part of `a88b0b3b`, adapted to Lazydino/OpenRouter cache namespace handling and `JCODE_HOME`. |
| `1e6d1d69` | Mark static compatible model fallbacks | `HYBRID_REFACTOR` | Ported as part of `a88b0b3b`, with explicit route detail fallback marker and regression tests. |
| `93d1a52b` | Add inline edit expand collapse badge | `PORT_UPSTREAM` | Ported in follow-up B04 small-fixes commit: Alt+Shift+E toggles inline/full-inline and viewport shows expand/collapse badge. |
| `2d17d3f0` | Report provider profile config parse errors | `PORT_UPSTREAM` | Already present in current branch via prior provider catalog catch-up. Verified `Config::load_strict`, provider error propagation, and regression test. |
| `ddd3dea5` | Add Xiaomi MiMo provider support | `PORT_UPSTREAM` | Already present via prior provider catalog catch-up. Verified metadata/profile/static model entries. |
| `e9253e79` | Cover Xiaomi MiMo static model fallback | `PORT_UPSTREAM` | Already present via prior provider catalog catch-up. Verified `src/provider/openrouter_tests.rs` coverage. |
| `7d4e9983` | Add schedule tool cancel and list actions | `PORT_UPSTREAM` | Already present as `d4b17cfd Catch up upstream schedule tool actions`. |
| `b9aa33c2` | fix: silence auto-poke completion without todos | `PORT_UPSTREAM` | Already present as `a0468741 Catch up upstream auto-poke todo silence`. |
| `bbc6c99c` | test: guard against zero-test cargo filters | `PORT_UPSTREAM` | Already present as `d13295c2 Catch up upstream zero-test cargo guard`. |
| `dcc6e588` | test: update openai-compatible login copy | `SKIP_UPSTREAM` | Test-copy-only update overlaps with Lazydino's richer provider/login copy and harness routing tests. No runtime behavior needed. |
| `566fe4bf` | docs: update e2e split quality todo | `KEEP_OURS` | Upstream docs are not authoritative for Lazydino's custom catch-up/milestone docs. No port. |
| `c926f6d4` | test: isolate openrouter auth status env | `PORT_UPSTREAM` | Behavior covered by current auth/provider test env guards. No extra code needed. |
| `d724c852` | Reduce production panic paths | `PORT_UPSTREAM` | Ported in follow-up B04 small-fixes commit for provider HTTP fallback, auth runtime lookup, and provider fingerprint hashing. |
| `8d2ad349` | Split large launch and protocol modules | `KEEP_OURS` | Refactor-only. Direct cherry-pick conflicts with Lazydino desktop/protocol custom structure. Keep current structure until a dedicated module-extraction pass. |
| `08391573` | Extract desktop session IO and tests | `KEEP_OURS` | Refactor-only and desktop-specific. Current desktop tree differs and does not have upstream `session_launch/` layout. |
| `808536cf` | Extract provider metadata catalog | `HYBRID_REFACTOR` | Lazydino already has `crates/jcode-provider-metadata` with merged custom provider metadata. Keep current hybrid instead of re-extracting upstream file layout. |
| `f3b9b42e` | Extract markdown text preprocessing | `KEEP_OURS` | Lazydino markdown crate already has incremental/render support modules. Upstream extraction would duplicate/churn without needed runtime change. |
| `c59dcc48` | Extract protocol wire enums | `KEEP_OURS` | Lazydino protocol crate already has notifications/memory/tests layout. Direct upstream split conflicts with custom protocol work. |
| `4ef3607e` | Split overnight prompts and tests | `HYBRID_REFACTOR` | Lazydino `jcode-overnight-core` already contains prompt helpers/tests in a richer custom layout. Keep hybrid; no behavior gap found. |

## Validation performed

- `cargo fmt`
- `cargo fmt --check`
- `cargo check -q -p jcode`
- `cargo test -q -p jcode remote_compatible_route_ -- --nocapture`
- `cargo test -q -p jcode named_provider_profile_reports_malformed_config_instead_of_unknown_profile -- --nocapture`
- `cargo test -q -p jcode test_scheduled_queue_remove_by_id_persists_remaining_items -- --nocapture`
- self-dev build/reload for `a88b0b3b` remote route cache port

## Notes

The B04 remote compatible model cache port intentionally uses `jcode_provider_openrouter::load_disk_cache_entry_for_namespace` and makes the provider cache root honor `JCODE_HOME`. This keeps self-dev/test sandboxes isolated and avoids reading a user's real `~/.jcode/cache` during tests.
