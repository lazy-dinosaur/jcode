# B05 upstream catch-up decisions — 2026-05-24

Range audited: `4ef3607e..5b52a370` (20 upstream commits after B04).

## Summary

- `PORT_UPSTREAM`: 8
- `HYBRID_REFACTOR`: 3
- `KEEP_OURS`: 8
- `SKIP_UPSTREAM`: 1
- `NEEDS_REVIEW`: 0

B05 is closed for this catch-up pass. The runtime-affecting TUI CPU/UX/provider-copy changes were ported. Desktop-only rendering changes and extraction-only markdown/mermaid/mobile commits were deliberately kept as Lazydino structure because the local tree already has richer custom modules in those areas.

## Decisions

| Upstream commit | Subject | Decision | Local handling |
| --- | --- | --- | --- |
| `56efd93a` | Extract mermaid debug APIs | `KEEP_OURS` | Refactor/extraction-only. Lazydino currently keeps mermaid debug helpers in the existing renderer layout. No behavior gap found for TUI runtime. |
| `bdffeebe` | Extract markdown incremental renderer | `HYBRID_REFACTOR` | Already partially present locally via `#[path = "markdown_incremental.rs"]`. Keep current hybrid layout. |
| `678fa3ce` | Extract markdown types and mermaid fallback | `KEEP_OURS` | Refactor-only. Local markdown crate has custom mermaid renderer/fallback integration and default renderer feature work. Avoid layout churn in B05. |
| `94685d2a` | Extract mobile simulator scenarios | `KEEP_OURS` | Refactor-only. Local `jcode-mobile-core` already contains scenario state/tests; no runtime behavior needed for current TUI catch-up. |
| `98f45d9f` | Fix desktop model picker loading stalls | `HYBRID_REFACTOR` | Desktop/protocol-specific. Local desktop tree differs substantially; defer to a dedicated desktop catch-up block. |
| `4404b598` | Reduce idle TUI CPU polling | `PORT_UPSTREAM` | Ported: auth fast cache TTL 60s, deep idle 5s, idle donut disabled after deep idle, no cache TTL countdown redraw churn. |
| `8a172d3b` | Let never-streamed sessions reach deep idle | `PORT_UPSTREAM` | Ported: `time_since_activity()` falls back to `app_started` in both runtime and TuiState impl. |
| `5aea4ebf` | Improve config cache invalidation | `KEEP_OURS` | Lazydino already has M19 hot config cache with debounce, last-good fallback, force reload, and tests. Upstream narrows env fingerprint to explicit keys; local policy intentionally tracks all `JCODE_*` and is covered by `config_env_fingerprint_tracks_every_apply_env_override_var`. |
| `6ad1319e` | Smooth desktop and selfdev reload startup | `HYBRID_REFACTOR` | Desktop part deferred. Selfdev reload behavior superseded by local `c3f78995 Fix selfdev reload waiting on peer sessions`, which avoids agent-turn handoff stalls more directly. |
| `0791e34c` | Show OpenCode usage in info widget | `PORT_UPSTREAM` | Ported: `WidgetProviderKind::OpenCode`, OpenCode API-key auth label, cost-based usage. Added regression `test_info_widget_remote_opencode_shows_cost_based_usage`. |
| `a48ca345` | Keep bash commands visible in tool rows | `PORT_UPSTREAM` | Ported bash detail fallback line for narrow tool rows. Added regression `test_render_tool_message_keeps_bash_command_visible_when_row_is_narrow`. |
| `998a7403` | Release v0.12.3 | `SKIP_UPSTREAM` | Release metadata only. Lazydino dev branch versioning is managed separately. |
| `973b930f` | Stop idle remote TUIs from repainting at active FPS | `PORT_UPSTREAM` | Ported: remote bus event handler returns whether redraw is needed and idle/deep-idle redraw policy avoids active FPS for dormant remote sessions. |
| `588ff0cc` | Fix desktop inline code markdown rendering | `KEEP_OURS` | Desktop-only rendering. Deferred to desktop catch-up. |
| `d25156a1` | Expand desktop markdown rendering coverage | `KEEP_OURS` | Desktop-only tests/rendering. Deferred to desktop catch-up. |
| `5311b3bc` | Optimize desktop startup first paint | `KEEP_OURS` | Desktop-only startup rendering optimization. Deferred to desktop catch-up. |
| `ee9c9517` | Auto-install updates on startup | `KEEP_OURS` | Startup update policy is sensitive for Lazydino custom channels. Do not auto-install upstream release behavior in this catch-up branch. |
| `979c2220` | Clarify OpenAI-compatible static model fallback | `PORT_UPSTREAM` | Ported text from `fallback: not from live /models catalog` to `fallback: static provider model list` and updated test expectation. |
| `b2c8d3aa` | Reduce CPU for spinner-only TUI states | `PORT_UPSTREAM` | Ported status-area tracking and spinner-only redraw fast path in run, remote run, and interactive turn loop; refined passive-liveness redraw policy. |
| `5b52a370` | Fix inline diff expand badge feedback | `PORT_UPSTREAM` | Ported `Alt+Shift+E` badge key pulse and active/feedback styles. Added regression `test_expand_badge_shortcut_toggles_inline_diff_and_pulses_key`. |

## Validation performed

- `cargo fmt`
- `cargo fmt --check`
- `cargo check -q -p jcode`
- `cargo test -q -p jcode test_info_widget_remote_opencode_shows_cost_based_usage -- --nocapture`
- `cargo test -q -p jcode test_render_tool_message_keeps_bash_command_visible_when_row_is_narrow -- --nocapture`
- `cargo test -q -p jcode test_expand_badge_shortcut_toggles_inline_diff_and_pulses_key -- --nocapture`
- `cargo test -q -p jcode test_redraw_interval_uses_low_frequency_during_remote_startup_phase -- --nocapture`
- `cargo test -q -p jcode test_remote_bus_dictation_completion_ignores_other_session -- --nocapture`
- `cargo test -q -p jcode direct_openai_compatible_static_models_are_marked_as_fallback_before_live_catalog -- --nocapture`

## Notes

B05 leaves desktop-focused commits for a later desktop-specific catch-up because the Lazydino desktop tree has diverged significantly and direct cherry-picking would mix large UI architecture changes with TUI runtime catch-up. The current B05 code commit focuses on low-risk, high-value runtime behavior: lower idle CPU, better narrow tool row readability, OpenCode usage display, clearer provider fallback labeling, and inline expand badge feedback.
